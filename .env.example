# 复制为 .env 后填写（勿提交 .env）
# 本地可选：接入大模型时填写；构建/部署时注入（GitHub 请在 Settings → Secrets 中配置同名 Secret）
# 配置后用户打开游戏即可使用 LLM 文案；不配置则用户需自行在"设置 → AI 文案设置"中填写
API_KEY=
API_BASE_URL=https://api.openai.com/v1
API_MODEL=gpt-4o-mini
